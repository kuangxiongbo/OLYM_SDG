# 文件移动总结

## 移动时间
2025-01-23

## 移动统计

- **脚本文件**: 26 个 → `backup/scripts/`
- **文档文件**: 114 个 → `backup/reports/` 和 `backup/docs/`
- **测试文件**: 26 个 → `backup/tests/`
- **其他文件**: 10 个 → `backup/misc/`

**总计**: 176 个文件已移动到 backup 目录

## 保留的核心文件

### 根目录保留文件
- `app.py` - 主应用（模块化架构）
- `config.py` - 配置文件
- `start.py` - **统一启动脚本**（推荐使用）
- `启动服务器.sh` - Shell启动脚本
- `README_START.md` - 启动说明文档
- `requirements.txt` - Python依赖文件
- `env_template` - 环境变量模板

### 保留的目录结构
- `routes/` - 路由模块
- `models/` - 数据模型
- `services/` - 服务模块（核心服务文件）
- `utils/` - 工具模块
- `templates/` - HTML模板
- `static/` - 静态资源（CSS/JS）
- `tests/` - 测试目录（保留用于未来测试）
- `api/` - API模块
- `core/` - 核心模块
- `data/` - 数据目录

## 移动的文件分类

### 1. 测试文件 → backup/tests/
- 所有 `test_*.py` 文件
- `run_tests.py`
- `pytest.ini`

### 2. 修复和工具脚本 → backup/scripts/
- `fix_*.py` - 数据库修复脚本
- `fix_*.sh` - Shell修复脚本
- `fix_*.sql` - SQL修复脚本
- `check_*.py` - 检查脚本
- `diagnose_*.py` - 诊断脚本
- `migrate_*.py` - 迁移脚本
- `init_*.py` - 初始化脚本
- `monitor_*.py` - 监控脚本
- 日志相关脚本

### 3. 文档文件 → backup/reports/ 和 backup/docs/
- 所有报告文档（`*.md`）
- 架构设计文档
- 测试报告
- 修复报告
- 实施总结

### 4. 临时文件 → backup/misc/
- 旧的API文件
- 旧的路由文件
- 旧的数据库文件
- 旧的环境模板

## 启动方式

现在只有一个统一的启动脚本：

```bash
cd web_interface
python start.py
```

或使用Shell脚本：

```bash
cd web_interface
./启动服务器.sh
```

## 访问信息

- **访问地址**: http://localhost:5000
- **登录页面**: http://localhost:5000/api/auth/login
- **测试账号**: admin@sdg.com / admin123

## 说明

所有移动的文件都完整保留在 `backup/` 目录下，按类型分类存放。如果需要恢复任何文件，可以从相应的backup子目录中找到。

清理后的目录结构更加清晰，只保留核心应用文件和必要的配置文件。

